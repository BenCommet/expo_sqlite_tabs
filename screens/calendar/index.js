import { StyleSheet, Text, View } from 'react-native';

export const CalendarScreen = function({navigation}) {
    return(
        <View>
            <Text>Calendar Screen</Text>
        </View>
    )
}